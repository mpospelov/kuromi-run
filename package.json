{
  "name": "kuromi-run",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && GIT_CONFIG_COUNT=2 GIT_CONFIG_KEY_0=url.https://github.com/mpospelov/.insteadOf GIT_CONFIG_VALUE_0=https://github.com/mpospelov/ GIT_CONFIG_KEY_1=credential.helper GIT_CONFIG_VALUE_1='!gh auth git-credential' gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.184.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "playwright": "^1.60.0",
    "vite": "^6.4.3"
  }
}
